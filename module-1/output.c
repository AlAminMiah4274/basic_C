#include <stdio.h>
int main()
{   
    // int Rahim;
    // float CGPA;
    // char Grade;
    // scanf("%d %f %c", &Rahim, &CGPA, &Grade);
    // printf("Output is: %d %0.2f %c", Rahim, CGPA, Grade);

    // int num1, num2;
    // scanf("%d%% %d%%", &num1, &num2);
    // printf("Output is: %d%% %d%%", num1, num2);

    // int num1, num2;
    // char p;
    // scanf("%d%c %d%c", &num1, &p, &num2, &p);
    // printf("output is: %d%% %d%%", num1, num2);

    int a, b;
    scanf("%d%% %d%%", &a, &b);
    printf("output is: %d%% %d%%", a, b);
    return 0;
}