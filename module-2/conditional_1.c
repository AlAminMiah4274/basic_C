#include <stdio.h>
int main()
{
    int tk;
    scanf("%d", &tk);

    if(tk >= 400)
    {
        printf("I will take Burger today.");
    }
    else
    {
        printf("I won't take burger in next one month.");
    }

    return 0;
}

// one conditional statement 