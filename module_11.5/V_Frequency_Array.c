#include <stdio.h>
int main()
{
    int N, M;
    scanf("%d %d", &N, &M);

    int numbers[N];
    for (int i = 0; i < N; i++)
    {
        scanf("%d", &numbers[i]);
    }

    int counting_number[1] = {0};
    for (int i = 0; i < N; i++)
    {

    }

    return 0;
}